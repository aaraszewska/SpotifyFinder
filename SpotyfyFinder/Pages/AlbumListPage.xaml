<Page x:Class="SpotyfyFinder.Pages.AlbumListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SpotyfyFinder.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="800"
      Title="AlbumListPage" Loaded="OnPageLoaded">

    <Grid>
        

        <ListView x:Name="DataList" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0" Margin="10,0" PreviewMouseLeftButtonUp="OnLeftMouseButtonClick">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Background="#FFE6F1E1" Width="150" Height="200">
                        <Image Source="{Binding images[0].url}" Width="150" Height =" 150" Stretch="UniformToFill" VerticalAlignment="Top"/>
                        <TextBlock Text="{Binding name}" Margin="0,0,10,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
       
    </Grid>
</Page>
